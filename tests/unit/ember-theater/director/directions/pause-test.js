import Pause from 'ember-theater-director-pause/ember-theater/director/directions/pause';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | pause');

// Replace this with your real tests.
test('it works', function(assert) {
  var pause = Pause.create();
  assert.ok(pause);
});
